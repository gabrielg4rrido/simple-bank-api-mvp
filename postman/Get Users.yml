info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiURL}}/accounts"
  body:
    type: json
    data: ""

runtime:
  variables:
    - name: apiURL
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
