info:
  name: Get movements from account 1
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiURL}}/accounts/:accountId/movements"
  params:
    - name: accountId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
